# Default values for valkey-cart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: otel-demo

resources:
  requests:
    cpu: 10m
    memory: 16Mi
  limits:
    cpu:
    memory: 32Mi

swan_hpa:
  swan_minReplicas: 2
  swan_maxReplicas: 10