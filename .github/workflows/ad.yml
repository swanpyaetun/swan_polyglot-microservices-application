name: ad

on:
  push:
    branches:
      - main
    #paths:
    #  - 'src/ad/**'
  pull_request:
    branches:
      - main
    #paths:
    #  - 'src/ad/**'
  workflow_dispatch:

jobs:
  swan_docker:
    uses: ./.github/workflows/swan_docker.yaml
    with:
      swan_ecr_repository: "swan_polyglot-microservices-application/ad"
      swan_dockerfile_path: "src/ad/Dockerfile"
    secrets:
      SWAN_CI_ROLE_ARN: ${{ secrets.SWAN_CI_ROLE_ARN }}