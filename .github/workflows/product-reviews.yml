name: product-reviews

on:
  push:
    branches:
      - main
    #paths:
    #  - 'src/product-reviews/**'
  pull_request:
    branches:
      - main
    #paths:
    #  - 'src/product-reviews/**'
  workflow_dispatch:

jobs:
  swan_docker:
    uses: ./.github/workflows/swan_docker.yml
    with:
      swan_ecr_repository: "swan_polyglot-microservices-application/product-reviews"
      swan_dockerfile_path: "src/product-reviews/Dockerfile"
    secrets:
      SWAN_CI_ROLE_ARN: ${{ secrets.SWAN_CI_ROLE_ARN }}