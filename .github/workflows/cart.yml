name: cart

on:
  push:
    branches:
      - main
    #paths:
    #  - 'src/cart/**'
  pull_request:
    branches:
      - main
    #paths:
    #  - 'src/cart/**'
  workflow_dispatch:

jobs:
  swan_docker:
    uses: ./.github/workflows/swan_docker.yml
    with:
      swan_ecr_repository: "swan_polyglot-microservices-application/cart"
      swan_dockerfile_path: "src/cart/src/Dockerfile"
      swan_k8s_manifest_path: "swan_kubernetes/cart/deployment.yaml"
    secrets:
      SWAN_CI_ROLE_ARN: ${{ secrets.SWAN_CI_ROLE_ARN }}