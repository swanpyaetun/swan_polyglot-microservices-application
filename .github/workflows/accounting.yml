name: accounting

on:
  push:
    branches:
      - main
    #paths:
    #  - 'src/accounting/**'
  pull_request:
    branches:
      - main
    #paths:
    #  - 'src/accounting/**'
  workflow_dispatch:

jobs:
  swan_docker:
    uses: ./.github/workflows/swan_docker.yml
    with:
      swan_ecr_repository: "swan_polyglot-microservices-application/accounting"
      swan_dockerfile_path: "src/accounting/Dockerfile"
      swan_k8s_manifest_path: "swan_kubernetes/accounting/deployment.yaml"
    secrets:
      SWAN_CI_ROLE_ARN: ${{ secrets.SWAN_CI_ROLE_ARN }}