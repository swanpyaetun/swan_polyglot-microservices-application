name: kafka

on:
  push:
    branches:
      - main
    #paths:
    #  - 'src/kafka/**'
  pull_request:
    branches:
      - main
    #paths:
    #  - 'src/kafka/**'
  workflow_dispatch:

jobs:
  swan_docker:
    uses: ./.github/workflows/swan_docker.yml
    with:
      swan_ecr_repository: "swan_polyglot-microservices-application/kafka"
      swan_dockerfile_path: "/src/kafka/Dockerfile"
    secrets:
      SWAN_CI_ROLE_ARN: ${{ secrets.SWAN_CI_ROLE_ARN }}