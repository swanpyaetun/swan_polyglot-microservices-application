name: recommendation

on:
  push:
    branches:
      - main
    #paths:
    #  - 'src/recommendation/**'
  pull_request:
    branches:
      - main
    #paths:
    #  - 'src/recommendation/**'
  workflow_dispatch:

jobs:
  swan_docker:
    uses: ./.github/workflows/swan_docker.yaml
    with:
      swan_ecr_repository: "swan_polyglot-microservices-application/recommendation"
      swan_dockerfile_path: "/src/recommendation/Dockerfile"
    secrets:
      SWAN_CI_ROLE_ARN: ${{ secrets.SWAN_CI_ROLE_ARN }}